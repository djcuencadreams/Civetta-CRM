Hola, necesito que realices las siguientes modificaciones precisas para corregir el mapeo de datos de dirección en el formulario de envío (wizard) para clientes existentes. El problema es que los campos “Ciudad”, “Provincia” y “Referencia o Instrucciones Especiales para la Entrega” no se están llenando correctamente, ya que se están asignando valores equivocados (por ejemplo, “Ciudad” muestra la cédula y “Referencia” muestra el email). Por favor, ejecuta lo siguiente: en el endpoint /api/shipping/check-customer-v2, asegúrate de que la consulta al modelo de clientes recupere explícitamente las siguientes columnas, usando los nombres definidos en el esquema de la base de datos: street, city, province, delivery_instructions. Si la consulta devuelve estos campos en snake_case, pero prefieres trabajar en camelCase, modifica la respuesta para que el objeto customer tenga la propiedad deliveryInstructions igual a delivery_instructions; por ejemplo, en el objeto de respuesta, asigna: const customerResponse = { …customer, deliveryInstructions: customer.delivery_instructions }; confirma que el endpoint devuelva un objeto JSON en el que, por ejemplo, customer.city tenga el valor correcto, customer.province no esté vacío, y customer.deliveryInstructions contenga el valor esperado. En el componente client/src/components/shipping/ShippingLabelForm.tsx, dentro de la función que procesa la respuesta del endpoint (por ejemplo, searchCustomer), actualiza el mapeo de los campos de dirección para que se asignen correctamente. Específicamente, reemplaza el mapeo actual por lo siguiente: console.log(“Datos del cliente:”, data.customer); form.setValue(‘street’, data.customer.street || ‘’); form.setValue(‘city’, data.customer.city || ‘’); form.setValue(‘province’, data.customer.province || ‘’); form.setValue(‘deliveryInstructions’, data.customer.deliveryInstructions || data.customer.delivery_instructions || ‘’); Esto debe garantizar que el input de “Ciudad” reciba el valor de data.customer.city, el de “Provincia” el de data.customer.province, y el de “Referencia o Instrucciones Especiales para la Entrega” reciba data.customer.deliveryInstructions (o en su defecto data.customer.delivery_instructions). Una vez aplicados estos cambios, al buscar un cliente existente en el wizard, se debe visualizar correctamente la calle en el campo “Street”, la ciudad en el campo “City” (sin mostrar otros datos), la provincia en el campo “Province”, y las instrucciones especiales en el campo “Referencia o Instrucciones Especiales para la Entrega”. Además, el endpoint debe retornar la información correcta basada en el esquema definido en db/schema.ts. Por favor, aplica estos cambios y proporciona un reporte final indicando en qué archivos se realizaron las modificaciones y cómo verificaste que los datos se mapean y se muestran correctamente en el formulario. Gracias